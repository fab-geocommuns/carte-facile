export const mapOverlays = {
  cadastre: {
    neutral: {
      sources: { test: { type: 'vector' } },
      layers: [{ id: 'test-layer' }]
    },
    color: {
      sources: { test: { type: 'vector' } },
      layers: [{ id: 'test-layer' }]
    }
  }
}; 